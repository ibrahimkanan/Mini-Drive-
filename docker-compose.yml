version: '3.8'

services:
  # ⚙️ Backend Service
  app:
    container_name: mini-drive-app
    build: ./Server      # تأكد أن اسم المجلد عندك يبدأ بحرف كبير فعلاً
    ports:
      - "3000:3000"
    depends_on:
      database:
        condition: service_healthy
    restart: always
    environment:
      - PORT=3000
      # جمعنا متغيرات الداتابيس في رابط واحد أسهل لـ GORM
      - DB_URL=host=database user=postgres password=postgres dbname=minidrive_db port=5432 sslmode=disable
    volumes:
      - ./Server/uploads:/app/uploads

  # 🎨 Frontend Service (Next.js)
  client:
    container_name: mini-drive-client
    build: ./Client      # تأكد أن اسم المجلد عندك يبدأ بحرف كبير فعلاً
    ports:
      - "3002:3001"      # المتصفح: http://localhost:3002
    depends_on:
      - app
    restart: always
    environment:
      # هذا الرابط للمتصفح (Client Side)
      - NEXT_PUBLIC_API_URL=http://localhost:3000
      # تفعيل نظام مراقبة الملفات لويندوز (عشان التحديث التلقائي)
      - WATCHPACK_POLLING=true
    volumes:
      - ./Client:/app
      # 👇👇 خدعة التسريع (تمنع نسخ مكتبات النود للويندوز) 👇👇
      - /app/node_modules
      - /app/.next

  # 🗄️ Database Service
  database:
    image: postgres:15-alpine
    container_name: postgres-db
    ports:
      - "5435:5432"      # DBeaver Connect: localhost:5435
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: minidrive_db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
    driver: local